export enum UserEventType {
  DELETE = "DELETE",
  UPDATE = "UPDATE",
  EMAIL_VERIFICATION = "EMAIL_VERIFICATION",
  RESET_PASSWORD = "RESET_PASSWORD"
}

export enum HikeEventType {
  DELETE = "DELETE"
}

export enum HikeAgencyEventType {
  DELETE = "DELETE"
}

export enum HikeBookingEventType {
  DELETE = "DELETE",
  CREATE = "CREATE",
  UPDATE = "UPDATE"
}

export enum HikePaymentEventType {
  UPDATE = "UPDATE"
}

export enum HikeReviewEventType {
  CREATE = "CREATE"
}
