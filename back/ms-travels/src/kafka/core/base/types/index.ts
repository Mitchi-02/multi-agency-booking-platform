export enum UserEventType {
  DELETE = "DELETE",
  UPDATE = "UPDATE",
  EMAIL_VERIFICATION = "EMAIL_VERIFICATION",
  RESET_PASSWORD = "RESET_PASSWORD"
}

export enum TravelEventType {
  DELETE= "DELETE"
}

export enum TravelAgencyEventType {
  DELETE= "DELETE"
}

export enum TravelBookingEventType {
  DELETE= "DELETE",
  CREATE= "CREATE",
  UPDATE= "UPDATE"
}

export enum TravelPaymentEventType {
  UPDATE= "UPDATE"
}

export enum TravelReviewEventType {
  CREATE= "CREATE"
}
