export enum UserEventType {
  DELETE= "DELETE",
  EMAIL_VERIFICATION= "EMAIL_VERIFICATION",
  RESET_PASSWORD= "RESET_PASSWORD"
}

export enum TravelBookingEventType {
  DELETE= "DELETE",
  CREATE= "CREATE",
  UPDATE= "UPDATE"
}

export enum HikeBookingEventType {
  DELETE= "DELETE",
  CREATE= "CREATE",
  UPDATE= "UPDATE"
}

export enum TravelPaymentEventType {
  UPDATE= "UPDATE"
}

export enum HikePaymentEventType {
  UPDATE= "UPDATE"
}